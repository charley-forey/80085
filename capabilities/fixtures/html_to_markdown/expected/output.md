# Release notes

The **3.2** release adds [new docs](https://example.com/docs) and fixes *several* bugs in the `parser` module.

## Changes

- Faster startup
- Better errors

1. Upgrade the package
2. Restart the service

> Upgrading is safe. Downgrading is not.

```
pip install example==3.2
example --check
```

---

That is all.
